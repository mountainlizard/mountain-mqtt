# `embassy-poll-example-rp2040w`

This crate just contains the settings and build to run the `embassy-poll-example` example on an RP2040, e.g. on a Raspberry Pi Pico W board. See `embassy-poll-example/README.md` for instructions on running the example.
